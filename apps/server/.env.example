# D1 Database
DB_URL=            # 部署后由 wrangler 自动填充

# R2 Bucket
R2_BUCKET_NAME=    # 存储桶名称

# KV Namespace
KV_NAMESPACE_ID=   # naming space ID (自动注入)

# Admin Secret (可选)
ADMIN_SECRET=      # signing secret for CORS/CSRF

# Environment
ENVIRONMENT=       # development | production